import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: "Counter",
  description: "An example of DB operation",
};

export default function Counter() {
  return (
    <>
      {/* Counter content would go here */}
    </>
  );
}
